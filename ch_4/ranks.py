ranks = ["kingdom","phylum", "class", "order", "family"]
print(f"at the start : {ranks}")

# putting the ranks in reverse order with reverse() method
ranks.reverse()
print(f"after reversing : {ranks}")

# sorting the ranks in alphabetical order with sort() method
ranks.sort()
print(f"after sorting : {ranks}")